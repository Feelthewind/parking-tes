entity: User
items:
  user1:
    name: "{{name.firstName}}"
    email: "{{internet.email}}"
    password: "password"
  user2:
    name: "{{name.firstName}}"
    email: "{{internet.email}}"
    password: "password"
